version: 2

models:
  - name: stg_franchise
    description: '{{ doc("stg_franchise") }}'
    columns:
      - name: franchise_id
        description: "ID of the franchise"
        data_tests:
          - unique
          - not_null